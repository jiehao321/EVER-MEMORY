{
  "name": "evermemory",
  "version": "0.1.0",
  "private": true,
  "description": "EverMemory deterministic memory plugin for OpenClaw with intent and reflection baseline.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": "22.x"
  },
  "files": [
    "dist",
    "index.js",
    "index.ts",
    "openclaw.plugin.json",
    "plugin.json",
    "README.md",
    "docs/evermemory-installation-guide.md"
  ],
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "clean": "rm -rf dist dist-test",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "build:test": "rm -rf dist-test && tsc -p tsconfig.test.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "doctor": "node ./scripts/doctor.mjs",
    "openclaw:harden": "node ./scripts/openclaw-host-hardening.mjs",
    "quality:gate": "node ./scripts/quality-gate.mjs",
    "quality:gate:openclaw": "node ./scripts/quality-gate.mjs --with-openclaw --with-security",
    "quality:gate:feishu-qgent": "node ./scripts/quality-gate.mjs --with-openclaw --with-feishu-qgent --with-security",
    "test:openclaw:feishu-qgent": "node ./scripts/openclaw-feishu-qgent-e2e.mjs",
    "test:openclaw:smoke": "node ./scripts/openclaw-smoke.mjs",
    "test:openclaw:security": "node ./scripts/openclaw-security-gate.mjs",
    "test:unit": "npm run build:test && node --test dist-test/test/*.test.js",
    "test": "npm run test:unit",
    "validate": "npm run doctor && npm run check && npm run test:unit"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.48",
    "better-sqlite3": "^12.2.0"
  }
}
