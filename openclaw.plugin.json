{
  "id": "evermemory",
  "name": "EverMemory",
  "description": "Deterministic memory plugin for OpenClaw with governed write/recall behavior.",
  "version": "2.1.0",
  "kind": "memory",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Master switch for the EverMemory plugin. Set false to disable all memory operations without removing config."
      },
      "databasePath": {
        "type": "string",
        "description": "File path for the SQLite database (WAL mode). Use ':memory:' for ephemeral in-memory storage (no persistence). Default: .openclaw/memory/evermemory/store/evermemory.db (relative to OpenClaw context directory)."
      },
      "bootTokenBudget": {
        "type": "integer",
        "minimum": 1,
        "description": "Maximum token budget for the session boot briefing injected at sessionStart. Larger values surface more context but consume more context window. Default: 1200."
      },
      "maxRecall": {
        "type": "integer",
        "minimum": 1,
        "description": "Maximum number of memories returned by evermemory_recall per query. Higher values improve recall coverage but increase latency. Default: 10."
      },
      "debugEnabled": {
        "type": "boolean",
        "description": "Enable write_decision and retrieval debug event logging. Useful for diagnosing why memories were accepted or rejected. Adds minor write overhead. Default: true."
      },
      "semantic": {
        "type": "object",
        "additionalProperties": false,
        "description": "Semantic (vector) search configuration. Semantic search enables meaning-based recall beyond keyword matching.",
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Enable semantic vector search. When true, memories are embedded and a hybrid keyword+semantic retrieval is used. Requires @xenova/transformers (local) or OPENAI_API_KEY (openai provider). Default: true."
          },
          "maxCandidates": {
            "type": "integer",
            "minimum": 1,
            "description": "Maximum candidate memories fetched from the semantic index before re-ranking. Higher values improve recall at the cost of latency. Default: 30."
          },
          "minScore": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Minimum cosine similarity score [0–1] for a semantic candidate to qualify. Lower values surface more results; higher values are stricter. Default: 0.3."
          }
        }
      },
      "intent": {
        "type": "object",
        "additionalProperties": false,
        "description": "Intent classification configuration. Intent signals guide write/recall policy decisions.",
        "properties": {
          "useLLM": {
            "type": "boolean",
            "description": "Enable optional LLM-based intent enrichment for messages that exceed heuristic confidence. Adds one LLM call per ambiguous message. Default: false."
          },
          "fallbackHeuristics": {
            "type": "boolean",
            "description": "Fall back to rule-based heuristics when LLM intent classification is unavailable or disabled. Strongly recommended to keep true. Default: true."
          }
        }
      },
      "retrieval": {
        "type": "object",
        "additionalProperties": false,
        "description": "Retrieval scoring weight configuration. All weight groups are auto-normalized to sum to 1.0 — you only need to set relative values.",
        "properties": {
          "keywordWeights": {
            "type": "object",
            "additionalProperties": false,
            "description": "Scoring weights for keyword/structured retrieval ranking. Values are relative — they are normalized automatically.",
            "properties": {
              "keyword": {
                "type": "number",
                "minimum": 0,
                "description": "Weight for keyword match score (TF-IDF / Jaccard overlap). Higher values favor text similarity."
              },
              "recency": {
                "type": "number",
                "minimum": 0,
                "description": "Weight for recency (how recently the memory was updated). Higher values favor newer memories."
              },
              "importance": {
                "type": "number",
                "minimum": 0,
                "description": "Weight for the memory's importance score [0–1]. Higher values surface high-importance memories first."
              },
              "confidence": {
                "type": "number",
                "minimum": 0,
                "description": "Weight for the memory's confidence score [0–1]. Higher values surface high-confidence memories first."
              },
              "explicitness": {
                "type": "number",
                "minimum": 0,
                "description": "Weight for explicitness score [0–1]. Higher values favor explicitly stated facts over inferred ones."
              },
              "scopeMatch": {
                "type": "number",
                "minimum": 0,
                "description": "Bonus weight when a memory's scope exactly matches the query scope (userId, chatId, project). Increase to prefer local memories."
              },
              "typePriority": {
                "type": "number",
                "minimum": 0,
                "description": "Weight for memory type priority (identity > constraint > preference > ...). Higher values prefer higher-priority types."
              },
              "lifecyclePriority": {
                "type": "number",
                "minimum": 0,
                "description": "Weight for lifecycle priority (semantic > episodic > working). Higher values prefer mature memories."
              }
            }
          },
          "hybridWeights": {
            "type": "object",
            "additionalProperties": false,
            "description": "Blend weights for hybrid retrieval (keyword + semantic + base score). Values are normalized automatically.",
            "properties": {
              "keyword": {
                "type": "number",
                "minimum": 0,
                "description": "Weight of the keyword ranking component in hybrid blending."
              },
              "semantic": {
                "type": "number",
                "minimum": 0,
                "description": "Weight of the semantic (vector similarity) component in hybrid blending."
              },
              "base": {
                "type": "number",
                "minimum": 0,
                "description": "Weight of the base structural score (recency, importance, lifecycle) in hybrid blending."
              }
            }
          }
        }
      },
      "butler": {
        "type": "object",
        "additionalProperties": false,
        "description": "Butler Agent configuration. The Butler is a persistent episodic agent that provides strategic overlay, narrative tracking, and cognitive capabilities.",
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Enable the Butler Agent. When disabled, EverMemory operates in passive mode without strategic overlay or narrative tracking. Default: true."
          },
          "mode": {
            "type": "string",
            "enum": ["steward", "reduced"],
            "description": "Butler operating mode. 'steward' uses LLM for full cognitive capabilities (requires LLM gateway, not yet available in public SDK); 'reduced' uses heuristics only (no LLM calls). Default: reduced."
          },
          "cognition": {
            "type": "object",
            "additionalProperties": false,
            "description": "Cognitive engine (LLM) budget and timeout settings (effective only in steward mode).",
            "properties": {
              "dailyTokenBudget": {
                "type": "integer",
                "minimum": 1,
                "description": "Maximum tokens the Butler may consume per day across all LLM calls (effective only in steward mode). Default: 50000."
              },
              "sessionTokenBudget": {
                "type": "integer",
                "minimum": 1,
                "description": "Maximum tokens the Butler may consume per session (effective only in steward mode). Default: 10000."
              },
              "taskTimeoutMs": {
                "type": "integer",
                "minimum": 1,
                "description": "Timeout in milliseconds for a single cognitive task (effective only in steward mode). Default: 15000."
              },
              "fallbackToHeuristics": {
                "type": "boolean",
                "description": "Fall back to heuristic analysis when LLM is unavailable or budget is exhausted (effective only in steward mode). Default: true."
              },
              "modelTiers": {
                "type": "object",
                "additionalProperties": false,
                "description": "Model tier mapping for Butler cognitive tasks. Each tier maps to a provider and model. Unset tiers use the host agent defaults.",
                "properties": {
                  "cheap": {
                    "type": "object",
                    "additionalProperties": false,
                    "description": "Model used for low-cost cognitive tasks (e.g. keyword extraction).",
                    "properties": {
                      "provider": { "type": "string", "description": "Provider name (e.g. 'anthropic', 'openai')." },
                      "model": { "type": "string", "description": "Model identifier (e.g. 'claude-haiku-4-5-20251001')." }
                    }
                  },
                  "balanced": {
                    "type": "object",
                    "additionalProperties": false,
                    "description": "Model used for standard cognitive tasks (e.g. narrative assessment).",
                    "properties": {
                      "provider": { "type": "string", "description": "Provider name." },
                      "model": { "type": "string", "description": "Model identifier." }
                    }
                  },
                  "strong": {
                    "type": "object",
                    "additionalProperties": false,
                    "description": "Model used for complex cognitive tasks (e.g. strategic overlay).",
                    "properties": {
                      "provider": { "type": "string", "description": "Provider name." },
                      "model": { "type": "string", "description": "Model identifier." }
                    }
                  }
                }
              }
            }
          },
          "timeBudgets": {
            "type": "object",
            "additionalProperties": false,
            "description": "Wall-clock time budgets for Butler cycles at each hook.",
            "properties": {
              "sessionStartMs": {
                "type": "integer",
                "minimum": 1,
                "description": "Max time for Butler cycle at session_start (includes task drain). Default: 3000."
              },
              "beforeAgentMs": {
                "type": "integer",
                "minimum": 1,
                "description": "Max time for Butler cycle at before_agent_start (strategic overlay generation). Default: 2000."
              },
              "agentEndMs": {
                "type": "integer",
                "minimum": 1,
                "description": "Max time for Butler cycle at agent_end (result absorption). Default: 2000."
              }
            }
          },
          "attention": {
            "type": "object",
            "additionalProperties": false,
            "description": "Attention and insight surfacing configuration.",
            "properties": {
              "maxInsightsPerBriefing": {
                "type": "integer",
                "minimum": 1,
                "description": "Maximum number of insights surfaced per briefing cycle. Default: 3."
              },
              "tokenBudgetPercent": {
                "type": "number",
                "minimum": 0,
                "maximum": 1,
                "description": "Fraction of context window budget allocated to Butler overlay [0-1]. Default: 0.2."
              },
              "minConfidence": {
                "type": "number",
                "minimum": 0,
                "maximum": 1,
                "description": "Minimum confidence threshold for surfacing insights [0-1]. Default: 0.4."
              }
            }
          }
        }
      }
    }
  },
  "uiHints": {
    "databasePath": {
      "label": "Database Path",
      "help": "SQLite storage path for EverMemory",
      "advanced": true
    },
    "bootTokenBudget": {
      "label": "Boot Token Budget",
      "help": "Max tokens for the session boot briefing"
    },
    "maxRecall": {
      "label": "Max Recall",
      "help": "Maximum recall items for each query"
    },
    "debugEnabled": {
      "label": "Debug Events",
      "help": "Enable debug event logging for explainability"
    },
    "semantic.enabled": {
      "label": "Semantic Search",
      "help": "Enable vector-based semantic search (local or OpenAI)",
      "advanced": false
    },
    "semantic.minScore": {
      "label": "Semantic Min Score",
      "help": "Minimum cosine similarity [0–1] for semantic results",
      "advanced": true
    },
    "intent.useLLM": {
      "label": "Intent LLM Enrichment",
      "help": "Enable optional LLM intent enrichment",
      "advanced": true
    },
    "retrieval.hybridWeights.semantic": {
      "label": "Semantic Blend Weight",
      "help": "Relative weight of semantic score in hybrid retrieval",
      "advanced": true
    }
  }
}
