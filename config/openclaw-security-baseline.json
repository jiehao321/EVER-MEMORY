{
  "maxSummary": {
    "critical": 2,
    "warn": 6
  },
  "allowedCriticalCheckIds": [
    "security.exposure.open_groups_with_elevated",
    "security.exposure.open_groups_with_runtime_or_fs"
  ],
  "notes": [
    "This baseline tracks host-level OpenClaw security posture for regression control.",
    "Lower thresholds as upstream host configuration is hardened."
  ]
}
